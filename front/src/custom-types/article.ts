export interface Article {
    id: Number,
    title: String,
    createdAt: String,
    viewNum: Number,
    commentNum: Number,
    likeNum: Number,
    dislikeNum:Number,
    authorId: Number,
    nickname: String,
    imagePath: String,
}