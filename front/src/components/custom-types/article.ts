export interface Article {
    id: Number,
    title: String,
    createdAt: String,
    viewNum: Number,
    commentNum: Number,
    likeNum: Number,
    authorId: Number,
    nickname: String,
    imagePath: String,
}